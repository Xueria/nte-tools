# AGENTS.md — 代理协作规范

本文件定义 AI 代理在本仓库（`blind-tools`，Go + Fyne 桌面应用）中工作时的强制约定。
**本文件的规则优先于代理的默认习惯**，每一项任务都必须完整执行，不得省略或提前跳过。

| 编号 | 主题         | 一句话要求                                  |
|------|--------------|---------------------------------------------|
| 1    | 分支前置检查 | 每次动手前确认不在 `master`，否则停止并等待 |
| 2    | 代码质量     | 结构清晰、职责分明、风格统一                |
| 3    | 版本控制     | 一次修改一次 commit，提交前先 `git add .`   |
| 4    | 构建与验收   | 代理不自行构建，须发起提问并等待人工反馈    |
| 5    | 标准工作流   | 上述规则的执行顺序总览                      |

---

## 1. 前置检查：确认当前分支

**任何**文件改动（含文档、配置）开始之前，必须先执行：

```powershell
git rev-parse --abbrev-ref HEAD
```

根据结果分支处理：

| 当前分支                  | 处理方式                                                               |
|---------------------------|------------------------------------------------------------------------|
| `master`                  | **立即停止**，不得写入任何内容；向用户发起等待请求，待分支就绪后再继续 |
| 其他分支（如 `feat/xxx`） | 直接进入后续流程                                                       |

在 `master` 上发起的等待请求须包含以下信息：

- 当前位于 `master` 分支，按本规范不可直接修改；
- 请用户创建或切换到工作分支，例如 `git switch -c feat/xxx`；
- 明确说明代理将在此暂停，分支就绪后继续。

> **禁止**为保证任务进度而在 `master` 上先改后切，**禁止**代理自行命名并创建分支。

---

## 2. 代码质量：结构清晰

代码须易于阅读与维护，具体要求：

- **单一职责**：一个文件、一个函数只承担一项职责，职责边界明确。
- **分层清晰**：数据加载与领域模型置于 `model/`，界面与交互逻辑置于 `view/`，程序入口与命令行置于 `cmd/`；不得跨层直接耦合。
- **命名达意**：命名须体现用途与语义，禁止 `a`、`tmp`、`data1` 一类无意义名称。
- **控制函数规模**：函数过长须拆分为若干小函数，避免过深嵌套。
- **消除重复**：重复逻辑抽取为公共函数，不得复制粘贴。
- **风格统一**：与相邻代码的命名习惯、错误处理方式、注释风格保持一致。
- **改动最小化**：不进行与当前任务无关的重构、格式化或代码重排。

---

## 3. 版本控制：一次修改一次 commit

### 3.1 提交流程

每完成一处修改后，按以下顺序提交：

1. **复查改动**：执行 `git status`、`git diff`，确认改动符合预期且无调试残留；
2. **暂存改动**：执行 `git add .`；
3. **提交**：执行 `git commit -m "<type>: <简要说明>"`；
4. **汇报**：向用户说明 commit 号与本次改动摘要。

> **前提条件**：本规范要求以 `git add .` 暂存整个工作区，因此**开始一处修改前，工作区应仅包含本次任务相关的改动**。若工作区中已存在其他未完成的改动，须先向用户确认是否一并提交，不得擅自混合。

### 3.2 提交粒度

- **一次修改 = 一次 commit**：改动自包含，可独立回滚。
- 不得将多个互不相关的改动合并为一次提交。

### 3.3 commit message 格式

统一使用 `type: 简要说明`：

```text
feat: 新增盲盒价格解析
fix: 修复加载本地数据时的空指针
refactor: 拆分 auction 求解逻辑
docs: 添加 AGENTS.md 代理协作规范
```

---

## 4. 构建与验收：由用户手动完成

当任务推进到需要构建、编译、运行或验证的阶段时：

1. **代理不得自行执行构建或测试**；
2. 代理必须**发起一个交互式提问**（可选项式提问，而非仅在正文里告知一句话），等待用户手动构建测试后作答；
3. 该提问须至少提供下列两个选项：
   - **无错误 / 无 bug**：用户确认构建与运行均正常；
   - **填写错误日志**：用户选择该项并粘贴编译错误、运行异常堆栈或复现步骤；
4. 在用户作答之前，代理**保持暂停**，不得继续改动或推进任务；
5. 用户选择「无错误 / 无 bug」后，方可进入下一步；
6. 用户提交错误日志时，代理先定位并修复问题，再按本节重新发起同一提问，循环直至用户确认无错误为止。

提问措辞统一为：

> 是否有错误或者 bug？

---

## 5. 标准工作流

```text
1. git rev-parse --abbrev-ref HEAD   → 若为 master：停止并等待用户创建 / 切换分支
2. 确认本次改动范围                  → 确保工作区仅包含本次任务相关改动
3. 按第 2 节的约定编写代码            → 结构清晰、风格统一、改动最小
4. git status / git diff             → 复查改动内容
5. git add .                         → 暂存全部改动
6. git commit                        → 提交一次，并汇报 commit 号与改动摘要
7. 需要构建时：发起提问“是否有错误或者 bug？”
   → 等待用户选择「无错误」或填写错误日志 → 按反馈继续或修复
```
