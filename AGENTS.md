# AGENTS.md

通用编码流程。以下约定优先于代理默认习惯，每次任务都须执行。

## 1. 分支前置检查

改动前先执行 `git rev-parse --abbrev-ref HEAD`：

- 默认分支（`master`）：立即停止，向用户提问说明需创建或切换分支（如 `git switch -c feat/xxx`），待分支就绪后继续。
- 其他分支：继续。

不得先改后切，不得自行创建分支。

## 2. 代码质量

- 单一职责，职责边界明确，不跨层耦合。
- 命名达意，禁止 `a`、`tmp`、`data1` 一类名称。
- 函数过长须拆分，避免深层嵌套。
- 重复逻辑抽为公共函数。
- 与相邻代码的风格、错误处理、注释保持一致。
- 不做与任务无关的重构、格式化或重排。

## 3. 版本控制

一次修改 = 一次 commit，自包含、可独立回滚，不合并互不相关的改动。

1. `git status`、`git diff` 复查改动；工作区应只含本次改动，否则先向用户确认。
2. `git add .`
3. `git commit -m "type: 说明"`，type 如 `feat:`、`fix:`、`refactor:`、`docs:`。
4. 汇报 commit 号与改动摘要。

## 4. 构建与验收

代理不自行构建或测试。需要验证时发起可选项提问，措辞为「是否有错误或者 bug？」，不带多余说明：

- `无错误 / 无 bug`：继续下一步。
- `填写错误日志`：等待用户粘贴编译错误、异常堆栈或复现步骤，修复后重新发起同一提问。

用户作答前保持暂停。

## 5. 执行顺序

检查分支 → 确认工作区只含本次改动 → 写代码 → 复查提交 → 需要构建时提问。
