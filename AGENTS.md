# AGENTS.md

本文件定义 AI 代理在本仓库（`blind-tools`，Go + Fyne 桌面工具）工作时的强制约定。
**下列规则优先级高于代理的默认习惯**，每次任务都必须完整走一遍。

---

## 1. 写之前：检查分支（第一步，不可跳过）

任何代码或文件修改开始前，先执行：

```powershell
git rev-parse --abbrev-ref HEAD
```

判断与动作：

| 当前分支 | 动作 |
| --- | --- |
| `master` | **立即停止**，不要写任何代码，向用户发起等待请求，等待用户创建或切换分支后再继续 |
| 其他分支（如 `feat/xxx`） | 直接继续后续步骤 |

在 `master` 上发起的等待请求必须说明：

- 当前处于 `master` 分支，按约定不能直接修改；
- 请用户创建或切换到分支（例如 `git switch -c feat/xxx`）；
- 分支就绪后代理才继续写。

> 不得为了“推进任务”而在 `master` 上先写后切，也不得自行猜测分支名并创建。

---

## 2. 代码需要结构清晰

- **单一职责**：一个文件 / 函数只做一件事，职责边界写清楚。
- **分层明确**：数据加载与模型放 `model/`，界面与交互放 `view/`，入口与命令行放 `cmd/`；不要跨层直接耦合。
- **命名达意**：命名体现用途，不用 `a`、`tmp`、`data1` 之类无意义名字。
- **控制函数规模**：函数过长就拆分为若干小函数，避免超长嵌套。
- **消除重复**：重复逻辑抽成公共函数，不复制粘贴。
- **保持既有风格**：与周围代码的风格、错误处理方式、命名习惯保持一致。
- **改动最小化**：不做与当前任务无关的重构、格式化或重排。

---

## 3. 每次修改都提交一次 commit

- **一次修改 = 一次 commit**，改动自包含、可单独回滚。
- 提交前先看 `git status` 与 `git diff`，**只提交本次修改涉及的文件**，不夹带无关改动或他人未完成的改动。
- commit message 使用 `type: 简要说明` 形式，例如：

  ```text
  feat: 新增盲盒价格解析
  fix: 修复加载本地数据时空指针
  docs: 添加 AGENTS.md 代理协作规范
  refactor: 拆分 auction 求解逻辑
  ```

- 提交完成后再向用户汇报 commit 号与本次改动摘要。

---

## 4. 需要构建时：先提问，等用户手动测试

当任务进行到需要构建 / 编译 / 运行 / 验证的阶段时：

1. **代理不要自行构建测试**；
2. 向用户发起一个提问，明确询问：

   > 是否有错误或者 bug？

3. 然后**等待用户手动构建测试**并把结果反馈回来；
4. 用户反馈无错误 / 无 bug 后，才继续下一步；
5. 若用户反馈存在错误或 bug，先修复，再重复第 3 步。

---

## 5. 标准工作流

```text
1. 检查当前分支           → 若为 master，停止并等待用户创建/切换分支
2. 明确本次改动范围        → 确认只做这一件事
3. 按“结构清晰”的约定写代码
4. git status / git diff  → 只暂存本次相关文件
5. 提交一次 commit
6. 需要构建时：提问“是否有错误或者 bug？” → 等待用户手动构建测试 → 根据反馈继续或修复
```
