# AGENTS.md

本仓库为 `blind-tools`，Go + Fyne 桌面应用。以下约定优先于代理默认习惯，每次任务都须执行。

## 1. 分支前置检查

任何改动前先执行 `git rev-parse --abbrev-ref HEAD`：

- `master`：立即停止，向用户发起等待提问，说明需创建或切换分支（如 `git switch -c feat/xxx`），待分支就绪后继续。
- 其他分支：继续。

不得在 `master` 上先改后切，不得自行创建分支。

## 2. 代码质量

- 单一职责，职责边界明确。
- 分层：`model/` 数据与模型，`view/` 界面与交互，`cmd/` 入口与命令行，不跨层耦合。
- 命名达意，禁止 `a`、`tmp`、`data1` 一类名称。
- 函数过长须拆分，避免深层嵌套。
- 重复逻辑抽为公共函数。
- 与相邻代码的风格、错误处理、注释保持一致。
- 不做与任务无关的重构、格式化或重排。

## 3. 版本控制

每处修改提交一次，流程：

1. `git status`、`git diff` 复查改动；
2. `git add .` 暂存；
3. `git commit -m "type: 说明"`；
4. 汇报 commit 号与改动摘要。

- 一次修改 = 一次 commit，自包含、可独立回滚，不合并互不相关的改动。
- `git add .` 会暂存整个工作区，故动手前工作区应只含本次任务的改动；若已有其他未完成改动，先向用户确认是否一并提交。
- message 统一为 `type: 说明`，如 `feat:`、`fix:`、`refactor:`、`docs:`。

## 4. 构建与验收

代理不自行构建或测试。需要构建、运行或验证时：

1. 发起简洁的可选项提问，措辞为「是否有错误或者 bug？」，不带非必要括号或补充说明；
2. 选项：`无错误 / 无 bug`、`填写错误日志`（用户粘贴编译错误、异常堆栈或复现步骤）；
3. 用户作答前保持暂停，不推进任务；
4. 选「无错误」才进入下一步；提交错误日志则先修复，再重新发起同一提问。

## 5. 工作流

1. 检查分支，`master` 则停止等待。
2. 确认工作区只含本次改动。
3. 按第 2 节写代码。
4. `git status`、`git diff` 复查。
5. `git add .`。
6. 一次 commit 并汇报。
7. 需要构建时发起提问，等待作答后按反馈继续或修复。
